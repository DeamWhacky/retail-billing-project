spring.application.name=retail-billing

# MySQL Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/retail_billing?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=sam123
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect

# JWT
app.jwt.secret=RetailBillingSecretKey2026SuperSecureJWTSigningKey
app.jwt.expiration-ms=86400000

# Server
server.port=8080

# CORS
spring.mvc.cors.allowed-origins=http://localhost:5173
